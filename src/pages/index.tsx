import React from 'react';
import styles from './index.css';
import { formatMessage } from 'umi-plugin-locale';
import { Input } from 'antd';

export default function () {
  return (
    <div>
      <Input placeholder={'fsad'}></Input>
    </div>
  );
}
